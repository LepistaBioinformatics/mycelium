[package]
name = "mycelium-cli"
description = "Provide CLI ports to the mycelium project."

edition.workspace = true
authors.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
readme.workspace = true


# ? ---------------------------------------------------------------------------
# ? DEPENDENCIES
# ? ---------------------------------------------------------------------------

[dependencies]

myc-core = { version = "6.0.0", path = "../../core" }
mycelium-base = { version = "6.0.0", path = "../../base" }
mycelium-prisma = { version = "6.0.0", path = "../../adapters/prisma" }
mycelium-notifier = { version = "6.0.0", path = "../../adapters/notifier" }

env_logger.workspace = true
log.workspace = true
serde_json.workspace = true
tokio.workspace = true

clap = { version = "4", features = ["derive"] }
rpassword = "7.2.0"


# ? ---------------------------------------------------------------------------
# ? BINARIES
# ? ---------------------------------------------------------------------------

[[bin]]
name = "myc-cli"
path = "src/main.rs"
