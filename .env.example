# Docker Compose profiles when opening the devcontainer.
# Copy to .env at project root (or add to existing .env).
# See .devcontainer/README.md for other profiles (observability, security).
COMPOSE_PROFILES=default
