# Variables injected into containers (copy to .devcontainer/.env).
# COMPOSE_PROFILES belongs in the project root .env (see README.md).
MYC_VAULT_TOKEN="REPLACE_WITH_YOUR_VAULT_TOKEN"
MYC_REDIS_PASS="REPLACE_WITH_YOUR_REDIS_PASSWORD"
